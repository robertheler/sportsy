#gitignore is a list of files and folders that we do not upload to our repo
.DS_Store

#Our node_modules folder is installed when you run the install script so we don't need to save it in our repo
node_modules

#The bundle file is generated by webpack so we do not need to track this in our repo either
bundle.js

db/data/images
client/src/components/lcov-report/
client/src/components/lcov.info
client/src/components/coverage-final.json
client/src/components/clover.xml